
## 2026-02-27 03:14 EST — Ableton bridge session durable notes
- Built substantial `ableton-bridge` capability in workspace: WebSocket JSON-RPC server, plugin/device/param control methods, sidechain helpers, telemetry ingest, and global scale/key state scaffolding.
- Added and iterated Max for Live helper scripts (`BridgeTelemetry.js`, `telemetry_post.js`, `ScaleBridge.js`) and established workflow that JS sidecar files must be synced into:
  `/Users/ryantaylorvegh/Music/ABLETON/User Library/Presets/Audio Effects/Max Audio Effect`
- Added sync helper script in workspace: `ableton-bridge/sync-to-user-library.sh` to copy M4L JS files into the Ableton User Library Max Audio Effect folder.
- Confirmed live duck telemetry path works via HTTP ingest (`/duck-telemetry`) and bridge `get_duck_telemetry`; UDP path was unreliable in this setup.
- Confirmed global key/scale read/write bridge path can work through the existing single bridge device; polling/startup and Max patch wiring are the fragile points.
- User preference reinforced: wants continuous concrete progress updates during long tasks, and dislikes “waiting for confirmation” messages when action is already requested.
- User preference reinforced: when something fails, prefer agent-owned MCP/bridge fixes over bouncing setup burden back to user whenever possible.
- User frequently uses Xrider by Nuro Audio on vocals; successfully loaded on Vocal Test and set a good even-vocal starting configuration.
