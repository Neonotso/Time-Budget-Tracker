# 2026-03-20

## Cron routing audit: Sally vs Gus Telegram cross-thread issue
- Root cause was not just inbound Telegram duplication; several cron jobs had been migrated from Sally/main to Gus by patching `agentId` and `sessionKey`, but their delivery configuration was left in a mixed inherited state.
- This caused ambiguous/incorrect announce behavior for Gus-owned isolated cron jobs, contributing to messages seeming to come through the wrong Telegram thread.
- Live cron store is `~/.openclaw/cron/jobs.json`.
- Wrote audit docs:
  - `CRON_AUDIT_2026-03-20.md`
  - `CRON_REBUILD_PLAN_2026-03-20.md`

## Immediate cron containment applied
- Set these Gus-owned isolated cron jobs to `delivery.mode: none` as a containment step to stop surprise external announcements while rebuilding:
  - Bible readers SMS reminder dispatcher
  - Email receipts to budget (Amazon)
  - Nightly Venmo to bank transfer
  - Monthly PIER Report Report
  - After-lesson automation sweep

## OpenClaw config fix
- Fixed a syntax error in `~/.openclaw/openclaw.json`: Gus agent block was missing a comma before the `model` field.
- This was breaking/complicating CLI config reads and likely contributing to flaky tooling behavior.

## Clean cron rebuild started
- Rebuilt Bible readers SMS reminder dispatcher from scratch under Gus with explicit routing:
  - old job id: `3ac6a4e1-e96c-4247-aacd-3dcf67a0538e`
  - new job id: `e112ec98-1ec0-4f7b-b3ff-8775c482dbfb`
- New explicit delivery config:
  - `agentId: gus`
  - `sessionTarget: isolated`
  - `delivery.mode: announce`
  - `delivery.channel: telegram`
  - `delivery.to: 8557709372`
  - `delivery.accountId: gus`
- Manual test run succeeded; script returned `sent_count=0` / `NO_REPLY`, confirming the fresh job runs with explicit routing.

## Version control status
- Workspace `~/.openclaw/workspace` is a git repo.
- Overall setup folder `~/.openclaw` is not currently a git repo.
- Committed audit doc in workspace repo:
  - commit `6b622dc` — `Add cron audit for Sally/Gus routing issue`
- Ryan is interested in later creating a separate repo or tracked setup strategy for the wider OpenClaw configuration/migration story.
