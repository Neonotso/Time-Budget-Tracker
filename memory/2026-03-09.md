## 2026-03-09

- Budget transaction added from screenshot: Amazon Prime Video movie purchase (`$3.99` rounded to `$4`) in `💰 Transactions` as category `Entertainment`.
- Google Sheets credential usage that worked:
  - Load vars from `.secrets/google_sheets & drive.env`.
  - Build `google.oauth2.credentials.Credentials` with:
    - `token=None`
    - `refresh_token=GOOGLE_SHEETS_REFRESH_TOKEN`
    - `token_uri='https://oauth2.googleapis.com/token'`
    - `client_id` / `client_secret` from env
  - Call `creds.refresh(Request())` before building Sheets service.
  - Do **not** force scopes in this flow (avoids `invalid_scope` with this refresh token).
  - Append transaction via `spreadsheets().values().append(... valueInputOption='USER_ENTERED')`.

- Mission Control updates:
  - Added real Discord project/channel structure from screenshot into `mission-control` (excluding `#squirrely-fun` from project lanes).
  - Seeded project context summaries in dashboard for:
    - PIER Ableton Setup
    - PIER Map App
    - PIER Song Database
    - Slide Images for Church
    - Chord Charts
  - Plan confirmed: keep one shared brain/session, but present project-specific chat views in UI with category grouping (Ableton Live / PIER Church) plus separate Squirrely Chat.
  - Added additional seeded summaries from Ryan for: Ableton Live Control, iOS Ableton Remote, Backing Track Generation, Mission Control, and Monthly Budget.
