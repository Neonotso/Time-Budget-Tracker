
## 2026-03-02 23:41 EST — UI Automation Progress
- Confirmed Peekaboo has Accessibility + Screen Recording permissions granted.
- Successfully opened Ableton Live, clicked OK button on startup dialog using Peekaboo with `--no-auto-focus` flag.
- Tested typing in TextEdit and successfully saved a file ("sally test.rtf") to Desktop using the workflow:
  1. Open app
  2. Handle dialogs (click OK)
  3. Cmd+S to open Save As
  4. Type filename in text field
  5. Click Save button (must target correct app explicitly with `--app` flag)
- Key learning: Peekaboo click must use `--no-auto-focus` flag to avoid timeout errors on certain dialogs, and must explicitly target the correct app to avoid clicking wrong buttons.

## 2026-03-03 00:43 EST — Ableton Dialog Automation Success!
- **CRITICAL**: Use `--no-auto-focus` flag with peekaboo click for ALL dialog buttons - this is the key that makes automation work
- Workflow: Open Ableton file → handle dialogs (Yes/No/OK) → OSC works after dialogs dismissed
- OSC port 11000 works after startup dialogs are cleared
- **Element naming**: Use `elem_X` from peekaboo see output, not button text (e.g., `elem_3`, `elem_5`)
- Some Ableton files open with persistent "Help View" panel - doesn't block OSC, can ignore
- Project folder structure allows direct save (Cmd+S) without Save As dialog

### Startup dialog sequence for Ableton files:
1. First dialog: "Yes/No" about MIDI remote script changes → click Yes (`elem_5`)
2. Second dialog: "OK" about AbletonMCP → click OK (`elem_7`)
3. (Optional) Help View panel - ignore, doesn't affect OSC

### Click command template:
```bash
peekaboo click --app "Live" --on "elem_X" --no-auto-focus
```

## 2026-03-03 01:01 EST — Clicking ANY Dialog Button
- Successfully clicked buttons on multiple dialog types: Yes/No, OK, Help View close
- Key: ALWAYS use `--no-auto-focus` flag with peekaboo click
- Element IDs like `elem_3`, `elem_5`, `elem_7` work - use peekaboo see to find them
- If button has text (like "Yes", "OK"), try text first, fall back to elem_X
- Some dialogs are stubborn - clicking at coordinates (--coords X,Y) also works as fallback
- The help view panel that sometimes opens is harmless - doesn't block OSC commands

## 2026-03-03 02:10 EST — Dialog Box Awareness
- **ALWAYS check for dialog boxes** - especially when opening/closing files or saving
- Dialogs block ALL subsequent operations until dismissed
- Common Ableton dialogs: "Save changes?", "Audio disabled", "OK", "Yes/No"
- **Always click OK/Yes/No promptly** after opening a file or before saving
- Direct save workflow: Open template → edit → Cmd+Shift+S to "Save As" with new name → project folder auto-creates

## Full Sunday Set Build Procedure
1. Kill any existing Ableton: `pkill -f "Live"`
2. Open template: `open "/path/to/Sunday Template for OpenClaw.als"`
3. **Immediately check for dialogs** with `peekaboo see --app "Live"`
4. Handle dialogs in order: Yes → OK (both with `--no-auto-focus`)
5. Wait for OSC to respond (test with track/scene query)
6. Apply scene names via OSC `/live/scene/set/name`
   - Format: `* Full Song Name "ThreeWordName"` (short name max 3 words!)
7. Get song keys from Planning Center: `curl -u "id:token" "https://api.../plans/ID/items"`
   - key_name field contains the key
8. Get tempos from MultiTracks via browser relay
9. Save As: 
   - Cmd+Shift+S (opens Save As with filename field focused)
   - Type filename (e.g., "2026-03-22")
   - Cmd+Shift+G (Go to folder)
   - Type full path: `/Users/ryantaylorvegh/Music/Church/Backing Track Sets`
   - Enter (navigate to folder)
   - Enter (save)
10. Close properly: File > Close (or Cmd+W) - don't use `open` to reopen, use File > Open
11. Reopen via File > Open to verify

## 2026-03-03 11:56 EST — Save As Workflow + Scene Naming
- **Save As workflow** (key insight from Neonotso):
  - Cmd+Shift+S opens Save As dialog with filename field FOCUSED
  - Type filename first
  - Then Cmd+Shift+G for "Go to folder" 
  - Type full path to Backing Track Sets
  - Enter twice (once to navigate, once to save)
- **Scene name format**: `* Song Title "ThreeWordName"` (short name max 3 words!)
- **Opening files**: Don't use `open` command - it launches new instance. Use File > Open (Cmd+O) inside Live, or properly close current file first
