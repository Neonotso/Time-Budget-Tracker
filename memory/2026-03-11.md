
## Durable memory notes (compaction flush)
- Confirmed cross-session visibility is limited in this Discord thread: `sessions_list` returned only the current `#squirrely-fun` session, so no separate “other chat” session was accessible from this context.
- User preference/confirmation in this thread: when asked to check other sessions, run the check directly rather than just describing limitations.

## Afternoon updates
- Bible Readers web app footer update completed and deployed to the originally shared shortlink target (`da.gd/bibleread`) by updating the old Apps Script project directly:
  - Script ID: `1ge7_Kl9GanSLKqmWJBatOrp-qgY7a8fLvyUQ2ULWuoIIU4sPHBIqjvQ2`
  - Deployment ID: `AKfycbzgMSqmzXpKeQwtBNj44vordcoze1pA9m_HjJ3CgiJ5ZtehRXMqc_Y985W2ZMZj7Aaj`
- Added church footer content to signup form: Bauer Community Fellowship, 4852 Bauer Rd, Hudsonville MI 49426.
- Deployment tooling improved to support stable Apps Script deployment reuse (`scripts/deploy_bible_readers_webapp.py` + state file `scripts/.bible_readers_deploy.json`).

## War Quotes OCR workflow
- Initial OCR quality via Tesseract was poor for the quote images.
- Installed EasyOCR in workspace venv (`easyocr 1.7.2`) to improve image text extraction quality.
- Added high-quality OCR pipeline script: `scripts/high_quality_quotes_ocr.py` (rotation/PSM search, optional vision cleanup with `OPENAI_API_KEY`).
- Drive folder note: the relevant folder name is `War Quotes` (not "Work Quotes").
- Created and uploaded transcription file to Drive: `war_quotes_transcribed.txt`.
- Sent Ryan an email copy with quote text in body via AgentMail from `sallysquirrel@agentmail.to`.

## Google Photos API access status
- Multiple re-auth attempts completed including `photoslibrary.readonly` and full scope set.
- Token inspection shows Photos scope present, but API calls to `photoslibrary.googleapis.com` still return `403 insufficient authentication scopes` in this environment.
- Current practical fallback: use Drive-hosted images for OCR until Photos API issue is resolved.

## DistroKid / Song Vault
- Spawned subagent `distrokid-song-vault-retrieval`; completion reported successful continuation of Song Vault population.
- Subagent reported creating 15 new song notes and updating 2 existing notes in `~/Library/CloudStorage/Dropbox/My Songs`, plus coverage reports under workspace `memory/`.

## Communication preferences reinforced (Ryan)
- Do not get in the habit of offering Sally/OpenClaw services to other people unless Ryan explicitly asks.
- For email thread replies, default to reply-all so Ryan remains in the loop unless he requests otherwise.

## Discord cross-session reconciliation
- Pulled and reviewed multiple Discord project channels Ryan provided to recover cross-session context.
- Created consolidated snapshot file:
  - `memory/discord-cross-session-snapshot-2026-03-11.md`
- Key recovered continuity includes:
  - PIER song database full-history pipeline + Google Sheet publication/formatting
  - PIER map app navigation + UX + deploy cadence details
  - Mission Control prototype status and architecture context
  - Ableton lanes (weekly setup, bridge, iOS remote, control methods)
  - PIER-specific unresolved items (ChordPro pipeline) + Fooocus slides workflow
- Created consolidated operations doc: `memory/operating-playbook-v1.md` with lane ownership, non-negotiable rules, active system mappings, and delegation/status cadence.
- Preference note (time language): when late at night, Ryan may use "tomorrow" to mean the next waking day in his current mental context (often the immediate next calendar day from his perspective before sleep); confirm or account for this in scheduling reminders.
